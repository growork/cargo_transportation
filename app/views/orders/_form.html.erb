<%= form_with(model: order, local: true) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <p><%= f.text_field :width, placeholder: 'Ширина' %></p>
    <p><%= f.text_field :height, placeholder: 'Высота' %></p>
    <p><%= f.text_field :length, placeholder: 'Длина' %></p>
    <p><%= f.text_field :weight, placeholder: 'Общий вес' %></p>
    <%= f.hidden_field :coordinates_from %>
    <%= f.hidden_field :coordinates_to %>

    <%= f.submit %>
  </div>
<% end %>
